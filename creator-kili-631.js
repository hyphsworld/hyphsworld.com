(function(){'use strict';var follow=document.getElementById('followCreator'),share=document.getElementById('shareCreator'),toast=document.getElementById('worldToast'),audio=document.getElementById('creatorAudio'),play=document.getElementById('playTrack'),progress=document.getElementById('trackProgress'),shell=document.getElementById('trackProgressShell'),time=document.getElementById('trackTime'),title=document.getElementById('activeTitle'),meta=document.getElementById('activeMeta'),cover=document.getElementById('activeCover'),client,user,creator,isFollowing=false,busy=false,timer;if(!follow||!share||!toast||!audio||!play)return;function show(message){toast.textContent=message;toast.classList.add('show');clearTimeout(timer);timer=setTimeout(function(){toast.classList.remove('show')},2600)}function render(){follow.disabled=busy;follow.setAttribute('aria-pressed',String(isFollowing));follow.textContent=busy?'Please wait…':(isFollowing?'✓ Following KILI 631':'＋ Follow KILI 631')}function format(seconds){return Number.isFinite(seconds)?Math.floor(seconds/60)+':'+String(Math.floor(seconds%60)).padStart(2,'0'):'0:00'}function renderAudio(){var percent=audio.duration?Math.max(0,Math.min(100,audio.currentTime/audio.duration*100)):0;play.textContent=audio.paused?'▶':'❚❚';progress.style.width=percent+'%';shell.setAttribute('aria-valuenow',String(Math.round(percent)));time.textContent=format(audio.currentTime)}async function init(){render();document.getElementById('year').textContent=new Date().getFullYear();if(!window.HWAuth)return;try{client=await window.HWAuth.getClient();var result=await client.from('creators').select('id,slug,follower_count').eq('slug','kili-631').maybeSingle();if(result.error||!result.data)throw result.error||new Error('Creator unavailable');creator=result.data;var session=await client.auth.getSession();user=session.data&&session.data.session?session.data.session.user:null;if(user){var state=await client.from('creator_follows').select('creator_id').eq('creator_id',creator.id).eq('user_id',user.id).maybeSingle();if(state.error)throw state.error;isFollowing=Boolean(state.data)}render()}catch(error){console.warn('KILI 631 follow state unavailable.',error)}}follow.addEventListener('click',async function(){if(busy)return;if(!client||!creator){show('Creator connection is still loading');return}if(!user){show('Log in to follow creators');return}busy=true;render();try{var response=isFollowing?await client.from('creator_follows').delete().eq('creator_id',creator.id).eq('user_id',user.id):await client.from('creator_follows').insert({creator_id:creator.id,user_id:user.id});if(response.error&&response.error.code!=='23505')throw response.error;isFollowing=!isFollowing;show(isFollowing?'You’re following Creator #010':'Creator unfollowed')}catch(error){show('Could not update follow yet')}finally{busy=false;render()}});share.addEventListener('click',async function(){var data={title:'KILI 631 — Creator #010',text:'Enter KILI 631’s Bogotá Creator World and hear MULA with Hyph Life.',url:location.href};try{if(navigator.share)await navigator.share(data);else{await navigator.clipboard.writeText(location.href);show('Profile link copied')}}catch(error){if(error&&error.name!=='AbortError')show('Copy the profile address from your browser')}});play.addEventListener('click',function(){if(audio.paused)audio.play().catch(function(){show('Tap play again to start audio')});else audio.pause()});audio.addEventListener('timeupdate',renderAudio);audio.addEventListener('loadedmetadata',renderAudio);audio.addEventListener('play',renderAudio);audio.addEventListener('pause',renderAudio);shell.addEventListener('click',function(event){var rect=shell.getBoundingClientRect();if(rect.width&&audio.duration)audio.currentTime=Math.max(0,Math.min(1,(event.clientX-rect.left)/rect.width))*audio.duration});document.querySelectorAll('.track').forEach(function(button){button.addEventListener('click',function(){audio.pause();audio.src=button.dataset.src;cover.src=button.dataset.cover;title.textContent=button.dataset.title;meta.textContent=button.dataset.meta;audio.load();audio.play().catch(function(){show('Tap play to listen')})})});init()})();
