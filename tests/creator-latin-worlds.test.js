const fs=require('fs');const path=require('path');const root=path.resolve(__dirname,'..');const read=file=>fs.readFileSync(path.join(root,file),'utf8');const kili=read('creator-kili-631.html');const yko=read('creator-ykomusic.html');const language=read('creator-language.js');const directory=read('creators.html');const migration=read('supabase/migrations/20260926170000_add_ykomusic_creator.sql');
describe('Spanish-first Latin Creator Worlds',()=>{test('places a persistent ES/EN picker at the top of both pages',()=>{for(const page of[kili,yko]){expect(page).toMatch(/id="creatorLanguage"/);expect(page).toMatch(/<option value="es">ES/);expect(page).toMatch(/<option value="en">EN/);expect(page).toMatch(/creator-language\.js/)}});test('defaults both pages to Spanish while preserving English copy',()=>{expect(kili).toMatch(/<html lang="es">/);expect(yko).toMatch(/<html lang="es">/);expect(kili).toMatch(/data-es=/);expect(kili).toMatch(/data-en=/);expect(yko).toMatch(/data-es=/);expect(yko).toMatch(/data-en=/);expect(language).toMatch(/localStorage/)});test('embeds the supplied KILI 631 video safely',()=>{expect(kili).toMatch(/youtube\.com\/embed\/b3qhj6unwlI/);expect(kili).toMatch(/loading="lazy"/);expect(kili).toMatch(/allowfullscreen/)});test('publishes YKOMUSIC as #009 and preserves the JCrown identity',()=>{expect(yko).toMatch(/Creator #009/);expect(yko).toMatch(/ANTES CONOCIDO COMO JCROWN/);expect(yko).toMatch(/2PRuSUeadQFX8zBuuLummJ/);expect(directory).toMatch(/data-name="ykomusic jcrown"/);expect(directory).toMatch(/10 creators • Alphabetical/)});test('tracks the YKOMUSIC row as featured, not automatically verified',()=>{expect(migration).toMatch(/\(9, 'ykomusic'/);expect(migration).toMatch(/'published', 'featured'/);expect(migration).not.toMatch(/verified artist/i)});});
