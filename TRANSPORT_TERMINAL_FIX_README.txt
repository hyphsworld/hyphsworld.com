HYPHSWORLD TRANSPORT + ACCESS TERMINAL FIX

FILES INCLUDED
- vault.html
- vault.css
- vault.js

WHAT THIS FIXES
1. Body scan still shows.
2. After access is granted, the overlay switches into a transport tunnel animation.
3. Auto navigation now uses a more reliable flow.
4. A manual fallback button appears inside the overlay in case auto-transport stalls.
5. The access terminal / pad is animated with rings, beam, grid, and console visuals.

UPLOAD THESE TOGETHER
1. vault.html
2. vault.css
3. vault.js

AFTER UPLOAD
- Hard refresh the page.
- Run the body scan.
- You should see:
  scan -> access granted -> transport tunnel -> navigation
- If navigation gets blocked or stalls, tap the "Enter Vault Manually" button.

NOTE
The code remains hash-checked and is not written in plain text in the JavaScript.
