const fs = require('fs');

function read(file) { return fs.readFileSync(file, 'utf8'); }
function assert(ok, message) { if (!ok) throw new Error(message); }

const html = read('dominos.html');
const casino = read('games.html');
const css = read('games.css');
const js = read('dominos.js');
const points = read('global-points-engine.js');
const migration = read('supabase/migrations/20260905223000_secure_domino_multiplayer.sql');

assert(html.includes('domino-pov-room'), 'Domino room must include the POV scene');
assert(!html.includes('pov-opponent-seat'), 'POV must not render a duplicate opponent card');
assert(css.includes('url("assets/games/01-domino-room-pov-v1.webp")'), 'POV must render the cinematic room artwork');
assert(html.includes('pov-player-hand'), 'POV must place the player hand in the foreground');
assert(casino.includes('<a class="room-btn" href="dominos.html">Enter 01 Domino Room</a>'), 'Casino Domino card must open the live Domino room directly');
assert(casino.includes('<a class="room-btn secondary" href="leaderboard.html">All Leaderboards</a>'), 'Casino must label the general leaderboard accurately');
assert(!casino.includes('data-room="dominoes" data-cost="15"'), 'Casino must not present the retired Domino preview buy-in');
assert(html.includes('pov-player-hud'), 'POV must include an opponent status HUD');
assert(html.includes('pov-points-hud'), 'POV must show Cool Points inside the game scene');
assert(html.indexOf('pov-action-dock') > html.indexOf('domino-pov-room') && html.indexOf('pov-action-dock') < html.indexOf('domino-radio'), 'POV action controls must stay inside the first-person scene');
assert(css.includes('perspective:1000px'), 'POV scene must use table depth');
assert(css.includes('.pov-player-hand .domino-tile.is-playable'), 'Playable tiles must be visually obvious');
assert(js.includes('function pipFace(value)'), 'Dominoes must render visual pips instead of text-only tiles');
assert(js.includes('povHudTiles'), 'Opponent tile count must be visible');
assert(js.includes('canPlay(tile, boardTiles)'), 'Hand must distinguish playable tiles');
assert(js.includes('create_domino_room'), 'Supabase room creation must remain connected');
assert(js.includes('claim_domino_win'), 'Verified Cool Points win award must remain connected');
assert(migration.includes('ordinality <= 7'), 'Regular bones must deal seven tiles per player');
assert(migration.includes('domino_hand_rank'), 'Highest double or highest pip bone must choose the opener');
assert(migration.includes("v_action = 'pass'"), 'Regular bones must support a legal pass');
assert(migration.includes('DRAW_UNTIL_PLAYABLE'), 'A player must draw while the boneyard has bones');
assert(js.includes('disabled aria-disabled'), 'Blocked hand bones must be natively disabled');
assert(js.includes('role="img"'), 'Board bones must expose an accessible image role');
assert(html.includes('passTurnBtn'), 'Table must include a pass control');
assert(html.includes('Regular Bones Rules'), 'Table must explain the standard rule set');
assert(css.includes('.domino-tile.is-double'), 'Doubles must turn sideways on the table');
assert(css.includes('aspect-ratio:9/15.8'), 'POV scene must use the tall phone-game composition');
assert(css.includes('overflow:auto;grid-template-columns'), 'Long live domino chains must remain scrollable instead of clipping');
assert(css.includes('grid-template-rows:1fr 1fr'), 'Player bones must stand vertically in the foreground');
assert(migration.includes('domino_safe_state'), 'Opponent hands must be masked by the server');
assert(migration.includes('STATE_CHANGED_REFRESH'), 'Concurrent moves must use optimistic version checks');
assert(migration.includes('game_scores_domino_room_winner_uidx'), 'A Domino room must award its win only once');
assert(css.includes('.domino-pov-room .pov-board{\n  overflow:auto'), 'Final POV board rule must preserve long-chain scrolling');
assert(css.includes('Consumer-readiness pass: the hand and dock always occupy separate tap zones.'), 'Final POV CSS must reserve separate hand and action zones');
assert(css.includes('grid-template-columns:repeat(7,minmax(0,1fr))'), 'Mobile rack must fit all seven bones inside the viewport');
assert(css.includes('.dominos-page #hwGlobalPointsHud{display:none!important}'), 'Global points HUD must not cover Domino controls');
assert(css.includes('01-domino-room-pov-female-v1.webp'), 'Female opponents must have a matching cinematic table plate');
assert(html.includes('povSelfAvatar'), 'The local player seat must reflect the selected profile avatar');
assert(js.includes('avatar_type,avatar_icon'), 'Opponent display must use safe public profile fields');
assert(js.includes('has-female-opponent'), 'Female opponent profiles must switch the table artwork');
assert(js.includes('opponentProfileRequest += 1'), 'Leaving a table must invalidate stale opponent profile requests');
assert(html.includes('data-hide-global-points-hud'), 'Domino page must opt out of the floating account widget');
assert(points.includes("hasAttribute('data-hide-global-points-hud')"), 'Points engine must honor page-level HUD suppression after injection');
assert(css.includes('.domino-tile:nth-child(n){box-sizing:border-box;width:100%'), 'Final mobile rack rule must neutralize edge-bone fan transforms');
assert(html.includes('dominoRadioAudio') && js.includes('initTableRadio'), 'Domino table radio must be wired without autoplay');
assert(html.includes('Start Table') && html.includes('2-Player Dominoes'), 'Domino multiplayer setup must be one-tap and accurately labeled');
assert(css.includes('.domino-pov-room>.pov-action-dock{\n  position:relative'), 'Controls must remain in flow below the tappable hand');
assert(css.includes('.domino-pov-room{\n  aspect-ratio:auto;\n  height:auto'), 'POV room must grow around the in-flow action dock');
assert(js.includes('opponentId && activeState.turnUserId === opponentId ? "PLAYING" : "WAITING"'), 'Opponent HUD must identify the active turn correctly');
assert(js.includes('setText("povHudName", "OPEN SEAT")'), 'Leaving a table must clear the opponent HUD');
assert(js.includes('join_domino_room'), 'Room joining must use the atomic server RPC');
assert(js.includes('domino_action'), 'Moves must use the versioned server RPC');
assert(js.includes('get_domino_state'), 'Refresh must use the opponent-safe state RPC');
assert(js.includes('list_domino_rooms'), 'Room discovery must use the authenticated room RPC');
assert(!js.includes('.from("game_state").upsert'), 'The browser must not overwrite Domino state directly');
assert(html.includes('data-game-key="01_dominos"'), 'Embedded scores must be restricted to Domino results');

console.log('Domino POV diagnostic passed: perspective scene, readable tiles, multiplayer, and points hooks are intact.');
