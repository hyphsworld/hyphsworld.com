/* HYPHSWORLD legacy compatibility shim.
   The only active wallet and floating points widget now live in global-points-engine.js. */
(function () {
  'use strict';
  window.HWCoolPointsLegacyDisabled = true;
})();
